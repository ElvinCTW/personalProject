extends layout
block content
  .main 
    .container
      .main-left
        .sub-main-container
          .subtext.subscribe 已配對物品
          - console.log(b_itemObjectArr.length)
          if b_itemObjectArr.length > 0
            each want in b_itemObjectArr
              .subscribe-item(onclick=`getMatchedResultData(${want.id}, '${want.title}', 'want')`)
                .subscribe-content 
                  span=want.title
          else
            .subscribe-item
              .subscribe-content 
                span 目前沒有配對
      .main-middle.matches-page
        //- .sub-main-container.news
          //- .subtext.news News
        .sub-main-container.recommands
          .subtext.recommand(id="subtext-matched-result-page") 配對結果
          .items-area.recommands(id="items-area-match")
            //- item div start here 
            //- - let n = 0;
            //- while n <6
            //-   .item-container-outside.recommands
            //-     .item-container-inside.recommands
            //-       .based-on
            //-         span Based on
            //-         span.item #Shoes
            //-       .item-img.recommands
            //-         img(src="", alt="" id="img")
            //-       .item.content
            //-         .title Title
            //-         .item-info 
            //-           span.nickname aaa
            //-           //- span.likes 10
            //-           span.status used
            //-         .tags tags
            //-           span tag
            //-   - n++
          .nomore-text-div(id="nomore-text-div") 請點選左方商品列查看更多配對
      //- .main-right
      //-   .sub-main-container
      //-   .subtext.ads Advertisement
      //-   .ads
      //-     img(src="/images/gad.png", alt="")
  script(src="/javascripts/match_check.js")
