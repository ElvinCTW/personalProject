extends layout
block content
  .main 
    .container
      .main-left
        .sub-main-container
          .subtext.subscribe 已確認的配對物品
          - console.log(getConfirmedMatchesResultArr.length)
          if getConfirmedMatchesResultArr.length > 0
            each match in getConfirmedMatchesResultArr
              .subscribe-item(onclick=`getMatchedResultData(${match.matched_id}, '${match.required_item_title}')`)
                .subscribe-content 
                  span=match.required_item_title
          else
            .subscribe-item
              .subscribe-content 
                span 目前沒有配對
      .main-middle.confirmed
        .sub-main-container.recommands
          .subtext.recommand(id="subtext-matched-result-page") 交易討論區
          .items-area.talk
            .msg-area(id="msg-area")
              .msg-line
                .msg-div 
                  .msg-topbar
                    .msg-name System
                    .msg-time 2020/02/16
                  .msg-content 請點選左邊配對物品，開啟對話視窗～
            .horizental-line
            .user-interact-area
              textarea.user-type-div(placeholder="請在此輸入訊息，最多250字元" name="user-type-content" id="user-type-content" maxlength="250")
              button.msg-send-btn(onclick="sendMsg()") 傳送訊息
      .main-right
        .subtext.ads 物品資訊(可捲動)
        .sub-main-container(id="main-right-inside-container")
          
  script(src="/javascripts/match_confirmed.js")
